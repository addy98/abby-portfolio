import { Container, Row, Col } from "react-bootstrap";

export default function PageSectionContent(props) {
    const section = props.section
    return (
        <div className="page-section-content">
            
        </div>
    )
}